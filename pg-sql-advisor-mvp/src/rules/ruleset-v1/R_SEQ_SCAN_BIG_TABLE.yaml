# src/rules/ruleset-v1/R_SEQ_SCAN_BIG_TABLE.yaml
id: R_SEQ_SCAN_BIG_TABLE
type: index
title: "Последовательное сканирование большой таблицы"
match:
  feature: seq_scan_big_table
action:
  rewrite_sql_hint: >
    Рассмотрите ограничивающее условие (WHERE) или индекс по селективной колонке.
    Если это аналитика по всей таблице — подумайте о партиционировании, материализации,
    или хранении агрегатов.
expected_gain:
  kind: io_reduction
  source: heuristic
risk:
  base: 10
effort: low
confidence: low